name: myabsensi
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  http: ^1.2.1
  shared_preferences: ^2.2.3
  geolocator: ^12.0.0
  image_picker: ^1.1.2 # TAMBAHKAN BARIS INI
  intl: ^0.19.0 # PASTIKAN BARIS INI ADA DAN INDENTASINYA BENAR
  geocoding: ^3.0.0 # Pastikan ini ada untuk geocoding (opsional, tapi bagus untuk alamat)

  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/ilustrator_login.jpg # Ini nama lama

